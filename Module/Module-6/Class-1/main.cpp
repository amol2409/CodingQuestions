#include <iostream>
#include "common.h"   // Include the header file where logVec is declared

int main()
{
    std::vector<int> vec = {1, 2, 3, 4};
    logVec(vec);  // Call the logVec function
    return 0;
}